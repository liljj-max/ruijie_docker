# 智慧零售赛题 · 下一步执行规划（技术文档 v1.0）

## 0. 文档信息
- 题目：DG-202606 面向智慧零售的自主服务机器人研发与应用
- 目标环境：官方 `supermarket_sorting:server` / `:client` 双镜像
- 本机：x86_64 i9-14900HX / 32GB / RTX 4060 8GB / Ubuntu 22.04
- 编制日期：2026-09-14
- 版本：v1.0（已确定加入 ArUco）

## 1. 目标与范围
在官方 Client 镜像中，用最小代价形成“接收任务 → 定位目标商品 → 抓取 → 送达配送台 → 循环”的闭环，稳定拿分。
- 第一目标：稳定完成一次有效取送（触发自动裁判得分）。
- 第二目标：一局完成 2~3 件。
- 第三目标：多随机种子下不崩溃、不失控。
- 明确加入 ArUco：用于货位确认与抓取前位姿微调，争取技术评价分。
- 不做：实机代码整体搬迁、MoveIt 移植、双臂协同、模型重训练、网页/systemd。

## 2. 当前基线状态（已验证）
| 项目 | 状态 |
| --- | --- |
| swap | 16G |
| Docker CE | 29.6.2，active（华为云镜像源） |
| NVIDIA Container Toolkit | 1.20.0，`daemon.json` 含 nvidia runtime |
| 镜像 | server `eb0b58a6…`、client `dbe0bfd2…`，与官方 PDF 摘要一致 |
| Baseline 源码 | `~/supermarket_sorting_baseline`（GitHub 官方仓库） |
| 固定 Baseline | 已跑通：nav→deploy→creep→close→lift→retreat→done，抓取保持 |
| GPU 验证 | 容器内 `nvidia-smi` 正常；Server 首次编译 gsplat 扩展约 90s（已缓存到卷） |
| 已知告警 | Server 缺 `/dev/dri`，GL 回退软件渲染（可加 `--device /dev/dri` 改善） |

## 3. 系统与技术原理

### 3.1 双镜像架构
```
Server 镜像（仿真真值 + ROS2 桥）
  ├─ MuJoCo 物理 + 3DGS 渲染
  ├─ 每局洗牌 45 商品到 45 货位、随机 5 障碍（A* 保通路）
  ├─ 商品 body 匿名化 item_run_xxxx_NN（每次启动变）
  ├─ 发布 task/sensors，订阅 control
Client 镜像（固定运行环境）
  └─ /workspace/baseline = 你的代码挂载点，entrypoint sleep，靠 docker exec 启动
通信：--network host + ROS_DOMAIN_ID=99 + RMW=rmw_cyclonedds_cpp
```

### 3.2 ROS2 接口
| 方向 | Topic | 类型 | 说明 |
| --- | --- | --- | --- |
| 收 | `/supermarket_sorting/task` | String | JSON，RELIABLE+TRANSIENT_LOCAL+depth1 |
| 收 | `/slamware_ros_sdk_server_node/scan` | LaserScan | 360 点，12Hz，frame=laser，0.02~12m |
| 收 | `/slamware_ros_sdk_server_node/odom` | Odometry | 底盘位姿/速度 |
| 收 | `/tf`、`/tf_static` | TFMessage | 动态/静态 TF |
| 收 | `/joint_states` | JointState | 17 关节，顺序固定 |
| 收 | `/head_camera/color/image_raw` + `camera_info` | Image/CameraInfo | 头部 RGB 640×480 |
| 收 | `/head_camera/aligned_depth_to_color/image_raw` + `camera_info` | Image/CameraInfo | 头部深度，mono16，毫米 |
| 收 | `/left_camera`、`/right_camera` 的 color | Image | 腕部 RGB（无深度） |
| 发 | `/cmd_vel` | Twist | 仅 `linear.x`、`angular.z`（非全向） |
| 发 | `/spine_forward_position_controller/commands` | Float64MultiArray | `[slide_joint]` |
| 发 | `/head_forward_position_controller/commands` | Float64MultiArray | `[head_yaw, head_pitch]` |
| 发 | `/left_arm_forward_position_controller/commands` | Float64MultiArray | 6 轴 + 夹爪 |
| 发 | `/right_arm_forward_position_controller/commands` | Float64MultiArray | 6 轴 + 夹爪 |
| 发 | `/aruco/<head|left|right>/ids|poses|detections` | 调试节点 | ArUco 货位确认 |

关节顺序：
```
slide_joint, head_yaw_joint, head_pitch_joint,
left_arm_joint1..6, left_arm_eef_gripper_joint,
right_arm_joint1..6, right_arm_eef_gripper_joint
```
夹爪命令 `1.0` 张开，Baseline 抓可乐用 `0.08` 闭合。

### 3.3 任务协议
```json
{"schema_version":1,"run_prefix":"run_xxx","count":5,
 "targets":[{"id":"item_run_xxx_01","kind":"kele"}, ...]}
```
- 只给 `id`（匿名）与 `kind`；不含 location/货架/层/列/aruco/坐标。
- 新 `run_prefix` 到达需清空旧库存与进度；不得跨局缓存。

### 3.4 随机化与防作弊
- 商品位置每局随机；`SUPERMARKET_SEED` 只复现物理布局，匿名 id 与 run_prefix 每次都变。
- ArUco 固定绑货位（A:0-8, B:9-17, C:18-26, D:27-35, E:36-44），商品↔货位↔ArUco 关系随机。
- 禁止读取 `retail_competition_layout.json` 真值、Server 内部坐标、未公开状态。

### 3.5 评分口径（官方 V2.0）
```
选手比分 = 资料分×0.3 + (任务分 + 技术评价分 − 扣除分)×0.7 + 裁判组打分
```
- 任务完成分 80 = 成功交付 60（12/件×5）+ 效率奖励 20（顺序优化 10 + 无间歇连续 10）
- 技术评价分 20 = 动态避障 10 + 视觉伺服/抓取放置 10（含 ArUco 利用程度）
- 扣除分：抓取掉落 −4；移动掉落 −3；定位不准碰邻位每次 −1（同物≤2）；失败循环清本轮；人工干预 −5；危险行为终止
- 自动裁判只看仿真物理结果，不检查代码；ArUco 只在技术评价分由人工评估
- 本地自测可用镜像内 `referee.py`（S1 到取货区→S2 够到→S3 夹住并撤出≥0.2m→S4 携物到配送区→S5 放进配送框+松开+静止<0.02m/s+倾角≤15°）
- 配送框真值区：`x[-2.42,-1.46] y[-3.63,-3.19] z[0.74,1.05]`，桌面高约 0.77m；默认时限 420s

## 4. 可复用资产
| 能力 | 文件 | 复用度 |
| --- | --- | --- |
| YOLO 检测+深度定位 | `Desktop/agx_arm_ros-ros2/src/agx_arm_vision/agx_arm_vision/yolo_grasp_node.py` | 高 |
| YOLO-World 权重 | 同目录 `models/yolov8s-worldv2.pt` | 高 |
| ArUco 货位对齐逻辑 | `shelf_workflow_node.py` + `config/shelf_layers.yaml` | 中高（需接官方 ArUco 话题） |
| 放置点规划 | `place_planner.py` | 中高 |
| 点云抓取兜底 | `pointcloud_grasp_node.py` | 中 |
| 抓取执行状态机 | `grasp_executor.py` | 中 |
| 任务/取送状态机骨架 | `ros2_ws_1/.../task_scheduler/pick_place_demo_node.py` | 中高（协议需换） |
| Nav2 参数思路 | `market_navigation2/config/nav2_params*.yaml` | 中 |
| 设计文档 | `桌面/rviz_pick_place_deliverables/取送放虚拟开发说明.md` | 高 |
| MMK2 控制/FK/IK | 官方 `supermarket_sorting_baseline` + 镜像示例 Client | 高（直接用） |

## 5. 总体技术路线
```
官方 Baseline 的 MMK2 控制/FK/IK
+ 我们 yolo_grasp_node 的视觉+深度+世界坐标（改话题/frame）
+ place_planner 的放置
+ pick_place_demo_node 的状态机骨架（换任务协议）
+ 官方 Server 自带 ArUco 话题（货位确认 + 抓取前微调）
= 比赛 Client MVP
```

## 6. 分阶段执行计划（含决策门）
| 阶段 | 内容 | 产出/验收 | 决策门 |
| --- | --- | --- | --- |
| P0 环境冻结 | 已完成部署与固定 Baseline 跑通 | 镜像 digest、话题清单、任务样例 | 已完成 |
| P1 任务+控制 | 订阅 task（transient-local 兼容 QoS）；解析字段；从 JointState.name 读关节；封装停车/头/升降/单臂/夹爪，加限幅+超时+home | 能打印正式任务；能发零速停车；能执行开爪/闭爪/home | 若控制封装不通，停止视觉开发 |
| P2 单次得分 | 基于官方示例 Client 的完整取送流程 | 触发一次有效取送并计分 | 第 8 小时门：未得分则回退，只修闭环 |
| P3 九类视觉 | YOLO-World + 9 kind 提示词，按任务 kind 过滤；深度有效性校验；多帧稳定锁定 | 随机种子下能识别 9 类 | 第 12 小时门：效果差则冻结高置信类别保底 |
| P4 货架扫描 | 固定观察点扫 A~E、三层；ArUco 定货位；视觉定 kind；建立 kind→候选货位映射 | 随机商品种子下至少找到 1 个任务商品 | 映射来源必须是相机+ArUco |
| P5 导航避障 | 优先 Nav2；1h 打不通则切 LaserScan 反应式避障 | ≥3 个障碍种子下往返；中断即零速 | 第 16 小时门 |
| P6 完整闭环 | 状态机 + 持物状态机 | 至少 1 件稳定成功，争取 2~3 件 | — |
| P7 随机测试 | 商品/障碍种子组合：11/3、23/17、47/31 | 记录识别/抓取/放置/碰撞/崩溃 | — |
| P8 封装提交 | 挂载 `-v <code>:/workspace/baseline:ro`；统一入口 | 一条启动命令；Ctrl+C 零速 | 第 21 小时功能冻结 |

## 7. 文件级移植清单
| 来源文件 | 动作 | 具体改动 |
| --- | --- | --- |
| 镜像 `examples/supermarket_sorting/supermarket_sorting_client.py` | 复制为基线 | 从 Server 镜像拷出（Client 镜像内无源码） |
| `yolo_grasp_node.py` | 复制+改 | `rgb_topic`→`/head_camera/color/image_raw`；`depth_topic`→`/head_camera/aligned_depth_to_color/image_raw`；`base_frame`→MMK2 `base_link`；`set_classes` 加 9 kind |
| `shelf_workflow_node.py` | 复制+改 | 订阅官方 `/aruco/head/ids|poses`；层高映射改用 MMK2 标定值 |
| `place_planner.py` | 复制+改 | frame 换 MMK2；放置目标=配送框真值区 |
| `pick_place_demo_node.py` | 借鉴重写 | 去掉 `/list_config`+`location_id`；改订阅 task；臂/夹爪改官方位置话题 |
| `grasp_executor.py` | 借鉴 | 保留状态机与结果码，动作层替换为 MMK2 位置控制 |
| `chassis_bridge/*`、`agx_arm_*`(MoveIt/CAN) | 不用 | 硬件绑定 |
| `models/yolov8s-worldv2.pt` | 打包 | 随提交目录，禁止运行时联网 |

## 8. 建议目录结构
```
/workspace/baseline/
├── competition_client/
│   ├── competition_client.py     # 主节点+状态机
│   ├── task_parser.py            # /supermarket_sorting/task 解析
│   ├── mmk2_adapter.py           # 底盘/头/升降/臂/夹爪封装+安全
│   ├── perception.py             # YOLO-World 9 类 + 深度
│   ├── aruco_detector.py         # 基于官方 ArUco 话题
│   ├── shelf_scanner.py          # kind→货位映射
│   ├── navigation.py             # Nav2 / 反应式避障
│   └── place_planner.py          # 放置
├── config/{competition,shelf_map,mmk2_poses,grasp_profiles}.yaml
├── models/yolov8s-worldv2.pt
├── scripts/run_baseline.sh
└── README.md
```

## 9. 安全与失败恢复规范
- 一次只处理一件；抓取后立即配送。
- 机械臂未收回禁动底盘；HOLDING 禁再抓取。
- 通信中断 → 持物状态置 UNKNOWN，禁止自动继续。
- 放置失败不得假设夹爪已空；抓取失败先安全回 home。
- 导航失败必须停车；所有运动有超时；Ctrl+C/异常发零速。
- 无深度/NaN/越界/陈旧图像拒绝抓取；连续多帧稳定才锁定。

## 10. 测试矩阵
| 用例 | 商品种子 | 障碍种子 | 关注 |
| --- | --- | --- | --- |
| 冒烟 | 固定 baseline | 固定 | 单次取送计分 |
| T1 | 11 | 3 | 找目标/抓取/放置 |
| T2 | 23 | 17 | 绕障/重规划 |
| T3 | 47 | 31 | 稳定性/崩溃 |
| 压力 | 11 | — | `SUPERMARKET_TASKS=all` 45 件 |

记录：是否收到任务、是否找到、货位是否正确、是否到达、是否抓取/放置成功、是否碰撞/掉落、是否崩溃。

## 11. 风险与决策门
| 风险 | 影响 | 对策 |
| --- | --- | --- |
| kind 任一件 vs 指定 body 口径不明 | 决定整个方案 | 最高优先级，尽快问主办方 |
| 8GB 显存不足 | 3DGS 渲染受限 | 降 `SUPERMARKET_USE_GS=0` 或 `GS_SEQUENTIAL=1` |
| 九类识别差 | 找不到目标 | 几何辅助分类，高置信类别保底 |
| Nav2 打不通 | 随机障碍失败 | 切 LaserScan 反应式避障 |
| ArUco 检测缺失 | 技术分损失 | 用官方 Server ArUco 话题补 |
| 时间不足 | 未闭环 | 第 8 小时前先拿一次有效分 |

## 12. 未决问题
1. 自动裁判是“抓到任意一件该 kind 即算”还是“必须指定匿名 body”？
2. 团队人数与分工（决定并行度）。
3. 提交形式：是否必须提交自建 Docker 镜像，还是挂载代码即可。

## 13. 交付物清单
- Docker 镜像/代码（挂载 `/workspace/baseline`）
- 部署说明文档
- 任务执行视频
- 作品介绍 PDF + PPT
- 统一启动入口 `scripts/run_baseline.sh`
